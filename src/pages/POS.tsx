import ProductCatalog from '../components/pos/ProductCatalog';

const POS = () => {
  return (
    <div className="min-h-screen bg-slate-50">
      <ProductCatalog />
    </div>
  );
};

export default POS;
